def cheat(Q1, Q3, Q4, Q6):
    Q1 = "import numpy as np"
    Q3 = "In Python, instead of creating a new object, it stores the new object in the same location as the old one.
    Yet in R, it stores the new object in a different location.
    To create a true new copy, type new_array = another_array.copy()"
    Q4 = "The %paste function does not work in a Python script. It is only defined for iPython terminal execution." \
         "in native Python, `%` is the modulo operator "
    Q6 =  "Type pip install pip-install-test using your terminal or %pip install pip-install-test in the iPython console"
    return(Q1, Q3, Q4, Q6)
